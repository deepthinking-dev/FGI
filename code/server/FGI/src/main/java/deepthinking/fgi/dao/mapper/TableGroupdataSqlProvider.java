package deepthinking.fgi.dao.mapper;

import java.util.List;
import java.util.Map;

import deepthinking.fgi.domain.TableGroupdata;
import deepthinking.fgi.domain.TableGroupdataCriteria;
import deepthinking.fgi.domain.TableGroupdataCriteria.Criteria;
import deepthinking.fgi.domain.TableGroupdataCriteria.Criterion;
import org.apache.ibatis.jdbc.SQL;

public class TableGroupdataSqlProvider {

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String countByExample(TableGroupdataCriteria example) {
        SQL sql = new SQL();
        sql.SELECT("count(*)").FROM("table_groupdata");
        applyWhere(sql, example, false);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String deleteByExample(TableGroupdataCriteria example) {
        SQL sql = new SQL();
        sql.DELETE_FROM("table_groupdata");
        applyWhere(sql, example, false);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String insertSelective(TableGroupdata record) {
        SQL sql = new SQL();
        sql.INSERT_INTO("table_groupdata");
        
        if (record.getGroupname() != null) {
            sql.VALUES("groupName", "#{groupname,jdbcType=VARCHAR}");
        }
        
        if (record.getGrouptype() != null) {
            sql.VALUES("groupType", "#{grouptype,jdbcType=INTEGER}");
        }
        
        if (record.getParentid() != null) {
            sql.VALUES("parentId", "#{parentid,jdbcType=INTEGER}");
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String selectByExample(TableGroupdataCriteria example) {
        SQL sql = new SQL();
        if (example != null && example.isDistinct()) {
            sql.SELECT_DISTINCT("id");
        } else {
            sql.SELECT("id");
        }
        sql.SELECT("groupName");
        sql.SELECT("groupType");
        sql.SELECT("parentId");
        sql.FROM("table_groupdata");
        applyWhere(sql, example, false);
        
        if (example != null && example.getOrderByClause() != null) {
            sql.ORDER_BY(example.getOrderByClause());
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String updateByExampleSelective(Map<String, Object> parameter) {
        TableGroupdata record = (TableGroupdata) parameter.get("record");
        TableGroupdataCriteria example = (TableGroupdataCriteria) parameter.get("example");
        
        SQL sql = new SQL();
        sql.UPDATE("table_groupdata");
        
        if (record.getId() != null) {
            sql.SET("id = #{record.id,jdbcType=INTEGER}");
        }
        
        if (record.getGroupname() != null) {
            sql.SET("groupName = #{record.groupname,jdbcType=VARCHAR}");
        }
        
        if (record.getGrouptype() != null) {
            sql.SET("groupType = #{record.grouptype,jdbcType=INTEGER}");
        }
        
        if (record.getParentid() != null) {
            sql.SET("parentId = #{record.parentid,jdbcType=INTEGER}");
        }
        
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String updateByExample(Map<String, Object> parameter) {
        SQL sql = new SQL();
        sql.UPDATE("table_groupdata");
        
        sql.SET("id = #{record.id,jdbcType=INTEGER}");
        sql.SET("groupName = #{record.groupname,jdbcType=VARCHAR}");
        sql.SET("groupType = #{record.grouptype,jdbcType=INTEGER}");
        sql.SET("parentId = #{record.parentid,jdbcType=INTEGER}");
        
        TableGroupdataCriteria example = (TableGroupdataCriteria) parameter.get("example");
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    public String updateByPrimaryKeySelective(TableGroupdata record) {
        SQL sql = new SQL();
        sql.UPDATE("table_groupdata");
        
        if (record.getGroupname() != null) {
            sql.SET("groupName = #{groupname,jdbcType=VARCHAR}");
        }
        
        if (record.getGrouptype() != null) {
            sql.SET("groupType = #{grouptype,jdbcType=INTEGER}");
        }
        
        if (record.getParentid() != null) {
            sql.SET("parentId = #{parentid,jdbcType=INTEGER}");
        }
        
        sql.WHERE("id = #{id,jdbcType=INTEGER}");
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table table_groupdata
     *
     * @mbg.generated
     */
    protected void applyWhere(SQL sql, TableGroupdataCriteria example, boolean includeExamplePhrase) {
        if (example == null) {
            return;
        }
        
        String parmPhrase1;
        String parmPhrase1_th;
        String parmPhrase2;
        String parmPhrase2_th;
        String parmPhrase3;
        String parmPhrase3_th;
        if (includeExamplePhrase) {
            parmPhrase1 = "%s #{example.oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{example.oredCriteria[%d].allCriteria[%d].value} and #{example.oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{example.oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{example.oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{example.oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        } else {
            parmPhrase1 = "%s #{oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{oredCriteria[%d].allCriteria[%d].value} and #{oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        }
        
        StringBuilder sb = new StringBuilder();
        List<Criteria> oredCriteria = example.getOredCriteria();
        boolean firstCriteria = true;
        for (int i = 0; i < oredCriteria.size(); i++) {
            Criteria criteria = oredCriteria.get(i);
            if (criteria.isValid()) {
                if (firstCriteria) {
                    firstCriteria = false;
                } else {
                    sb.append(" or ");
                }
                
                sb.append('(');
                List<Criterion> criterions = criteria.getAllCriteria();
                boolean firstCriterion = true;
                for (int j = 0; j < criterions.size(); j++) {
                    Criterion criterion = criterions.get(j);
                    if (firstCriterion) {
                        firstCriterion = false;
                    } else {
                        sb.append(" and ");
                    }
                    
                    if (criterion.isNoValue()) {
                        sb.append(criterion.getCondition());
                    } else if (criterion.isSingleValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase1, criterion.getCondition(), i, j));
                        } else {
                            sb.append(String.format(parmPhrase1_th, criterion.getCondition(), i, j,criterion.getTypeHandler()));
                        }
                    } else if (criterion.isBetweenValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase2, criterion.getCondition(), i, j, i, j));
                        } else {
                            sb.append(String.format(parmPhrase2_th, criterion.getCondition(), i, j, criterion.getTypeHandler(), i, j, criterion.getTypeHandler()));
                        }
                    } else if (criterion.isListValue()) {
                        sb.append(criterion.getCondition());
                        sb.append(" (");
                        List<?> listItems = (List<?>) criterion.getValue();
                        boolean comma = false;
                        for (int k = 0; k < listItems.size(); k++) {
                            if (comma) {
                                sb.append(", ");
                            } else {
                                comma = true;
                            }
                            if (criterion.getTypeHandler() == null) {
                                sb.append(String.format(parmPhrase3, i, j, k));
                            } else {
                                sb.append(String.format(parmPhrase3_th, i, j, k, criterion.getTypeHandler()));
                            }
                        }
                        sb.append(')');
                    }
                }
                sb.append(')');
            }
        }
        
        if (sb.length() > 0) {
            sql.WHERE(sb.toString());
        }
    }
}